name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print Name
        run: echo "Mamata"
      - name: final Message
        run: echo "Last Message"
